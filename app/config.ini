; This is the config file for the Roller Derby Test O'Matic
; This file is parsed and the values saved in global variables in config.php
; Rename this file config.ini to use

; mySQL database connection values

database_username = "docker"
database_userpassword = "docker"
database_name = "docker"
database_host = "database"

database_salt = ""

; SMTP server connection values

smtp_username = ""
smtp_userpassword = ""
smtp_host = ""

; Server domain

site_URL = "http://rdtom/"
